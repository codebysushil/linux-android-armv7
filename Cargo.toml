[package]
name = "android-rust-app"
version = "0.1.0"
edition = "2021"
authors = ["Sushil Kumar"]
description = "Rust Android multi-architecture example"
license = "Apache 2.0"

[lib]
name = "android_rust_app"
crate-type = ["cdylib", "staticlib"]

[dependencies]
napi = "2"
napi-derive = "2"
